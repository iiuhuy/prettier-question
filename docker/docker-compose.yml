version: '3.5'

services:
  antd_build:
    build: ../
    container_name: 'antd_build'
    volumes:
      - dist:/usr/src/app/dist

  antd_web:
    image: nginx
    ports:
      - 80:80
    container_name: 'antd_web'
    restart: unless-stopped
    volumes:
      - dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf

volumes:
  dist:
